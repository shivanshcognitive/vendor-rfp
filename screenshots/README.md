# Screenshots

Drop the 5 screen captures here (see the main README's "Screenshots"
section for exactly what each one should show and suggested filenames).
This folder is a placeholder — no images are pre-included because they
require a running instance (local or deployed) to capture.
