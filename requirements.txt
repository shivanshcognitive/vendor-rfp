streamlit>=1.35.0
pandas>=2.0.0
pdfplumber>=0.11.0
pydantic>=2.5.0
reportlab>=4.0.0
anthropic>=0.34.0
openai>=1.0.0

# LangGraph engine: models the same 10-step pipeline as an explicit
# typed-state graph. No hard version pin needed -- the StateGraph/
# add_conditional_edges/compile() API used here has been stable across
# recent LangGraph releases.
langgraph>=0.2.0
